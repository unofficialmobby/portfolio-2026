<nav class="floating-nav glass-panel">
  <a href="#top" class="nav-item" aria-label="Startseite">
    <span class="icon">⌂</span>
  </a>
  <a href="#journey-section" class="nav-item" aria-label="Mein Weg">
    <span class="icon">Route</span>
  </a>
  <a href="#project-showcase" class="nav-item" aria-label="Projekte">
    <span class="icon">Code</span>
  </a>
  <a href="#skills" class="nav-item" aria-label="Skills">
    <span class="icon">Skill</span>
  </a>
  <a href="mailto:deine-email@example.com" class="nav-item highlight" aria-label="Kontakt">
    <span class="icon">✉</span>
  </a>
</nav>

<style>
  .floating-nav {
    position: fixed;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    z-index: 100;
    
    display: flex;
    gap: 8px;
    padding: 10px 16px;
    border-radius: 100px; /* Pill-Shape */
    
    /* Wir nutzen deine globale Glass-Klasse, passen sie aber hier leicht an */
    background: rgba(20, 20, 20, 0.7) !important; 
    border: 1px solid rgba(255, 255, 255, 0.15) !important;
  }

  .nav-item {
    color: var(--color-text-secondary);
    text-decoration: none;
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 0.9rem;
    font-weight: 500;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .nav-item:hover {
    background: rgba(255, 255, 255, 0.1);
    color: var(--color-text-primary);
    transform: translateY(-2px);
  }

  /* Der Kontakt-Button sticht etwas hervor */
  .nav-item.highlight {
    background: var(--color-accent);
    color: #000; /* Schwarzer Text auf Blauem Button für Kontrast */
    font-weight: bold;
  }
  .nav-item.highlight:hover {
    box-shadow: 0 0 15px var(--color-accent);
  }

  /* Auf dem Handy verstecken wir den Text und zeigen nur Icons, um Platz zu sparen */
  @media (max-width: 600px) {
    .floating-nav {
      bottom: 1.5rem;
      padding: 8px 12px;
      width: 90%; /* Breiter auf Handy */
      justify-content: space-around;
    }
  }
</style>

<script>
  // Smooth Scroll Logik für die Links (falls Lenis mal kurz hakt)
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const targetId = this.getAttribute('href');
      // Spezialfall "top"
      if (targetId === '#top') {
        window.scrollTo({ top: 0, behavior: 'smooth' });
      } else {
        const target = document.querySelector(targetId);
        if (target) {
            target.scrollIntoView({ behavior: 'smooth' });
        }
      }
    });
  });
</script>